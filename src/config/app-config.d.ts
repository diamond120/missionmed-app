// src/config/app-config.d.ts

// You can define the types for your app-config module here.
// For example:
declare module "../config/app-config" {
  export const BASE_URL: string;
  // Add other exports as needed.
}
